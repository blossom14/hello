<% @friendships.each do |friendship| %>
	<% friend = friendship.user == current_user ? friendship.friend : friendship.user %>
	<%= link_to "chat with #{friend.name}", messages_friendship_path(friendship.id) %>
<% end %>